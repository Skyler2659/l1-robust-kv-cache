# 项目进度

更新时间：2026-06-17

## 当前状态

项目已经从临时实验堆叠整理成较清晰的论文实验工程结构。当前主线是：

- 在 MLX 4bit 后端上比较 KV cache eviction 策略。
- 核心研究问题是几何信号，尤其是 L1/L2 leverage，在缓存矩阵空间中是否能捕捉 attention 没有充分覆盖的重要 token。
- 当前最有力的已完成证据来自 RULER NIAH 和 RULER variable tracking。

## 已保留结果

当前 `results/` 下只保留两个完整结果目录，其它 smoke、partial、临时 LongBench、旧 quick 结果均已删除。

### RULER NIAH

路径：

```text
results/mlx_qwen25_7b_inst_4bit/ruler/20260611_225910
```

完整性：

- 900 条结果。
- 15 个方法。
- 3 个预算：128、256、512。
- 每个 method-budget setting 20 个样本。
- `dataset_official=true`。
- `skipped=0`，hook error 为 0。
- 图和分析文件已保留并重新验证可生成。

核心结果概览：

```text
full                 100 / 100 / 100
attention              0 /  80 / 100
snapkv                 5 /  35 /  90
pyramidkv              0 /   5 /  35
recency                0 /   0 /   5
random                 0 /   0 /   0
l1_leverage          100 / 100 / 100
l2_leverage          100 / 100 / 100
l1_prefill_only      100 / 100 / 100
l2_prefill_only      100 / 100 / 100
attention_l1         100 / 100 / 100
attention_l2         100 / 100 / 100
```

解释：这是当前最强的几何信号证据。它说明在 needle retrieval 这类证据 token 明确的任务里，leverage 类几何信号非常强，并且和 attention 存在明显互补。

### RULER Variable Tracking

路径：

```text
results/mlx_qwen25_7b_inst_4bit/ruler/20260615_223853
```

完整性：

- 240 条结果。
- 8 个方法。
- 3 个预算：256、512、1024。
- 每个 method-budget setting 10 个样本。
- `dataset_official=true`。
- `skipped=0`，hook error 为 0。
- 图和分析文件已保留并重新验证可生成。

核心结果概览：

```text
full              98 /  98 /  98
attention         56 / 100 / 100
snapkv            38 /  88 /  94
compactor          2 /  24 /  90
l1_prefill_only   92 /  86 /  96
l2_prefill_only  100 /  94 /  98
attention_l1      86 /  98 / 100
attention_l2      80 /  96 / 100
```

解释：VT 是比纯找针更接近状态跟踪的诊断任务。结果显示几何信号在 tight budget 下仍然强，attention 在预算变宽后追上。这支持一个更稳妥的叙事：几何信号是 tight-budget evidence guard，attention 是较宽预算下的语义选择器，二者可以互补。

## 已整理目录

正式配置：

```text
configs/experiments/paper/qwen25_7b/
```

诊断配置：

```text
configs/experiments/diagnostics/ruler/
configs/experiments/diagnostics/longbench/
```

消融配置：

```text
configs/experiments/ablations/niah/
```

矩阵配置：

```text
configs/experiments/matrix/
```

开发 sanity 配置：

```text
configs/experiments/dev/
```

旧配置归档：

```text
configs/experiments/archive/legacy/
```

模型配置：

```text
configs/models/
configs/models/hf/
```

eviction 配置：

```text
configs/eviction/
configs/eviction/groups/
```

## 已整理脚本

保留并统一命名的主脚本：

```text
scripts/run_paper_qwen25_7b_8subsets.sh
scripts/run_ruler_niah_qwen25_7b.sh
scripts/run_ruler_vt_qwen25_7b.sh
scripts/run_longbench_hotpotqa_qwen25_7b.sh
scripts/run_benchmark.py
scripts/run_analysis.py
scripts/plot_results.py
scripts/run_profile.py
scripts/smoke_test.py
```

已删除或归档：

- 临时 smoke config。
- strict/max8 HotpotQA prompt 探索配置。
- 旧 `final_8subsets` 非 official 配置移入 archive。
- 旧 `configs/experiment` 和 `configs/mlx` trial 配置移入 archive。

## Smoke 测试结果

已通过：

```bash
PYTHONPATH=. .venv/bin/python -m pytest tests/test_p0.py -q
```

结果：

```text
13 passed
```

已通过：

```bash
PYTHONPATH=. .venv/bin/python scripts/smoke_test.py
```

覆盖：

- CPU tiny NIAH。
- run_benchmark。
- run_analysis。
- run_profile。

已通过 MLX 15 方法 sanity：

```bash
TOKENIZERS_PARALLELISM=false PYTHONPATH=. .venv/bin/python scripts/run_benchmark.py \
  --config configs/experiments/dev/qwen25_05b_mlx_method_sanity.yaml \
  --skip_analysis
```

结果：

```text
15/15 methods completed
skipped = 0
hook_errors = 0
sanity_failed = 0
```

覆盖方法：

```text
full
random
recency
sink_recent
attention
snapkv
compactor
pyramidkv
l2_prefill_only
l2_key_prefill_only
l2_leverage
l1_prefill_only
l1_leverage
attention_l2
attention_l1
```

plot 验证：

- RULER NIAH 已重新生成 figures。
- RULER VT 已重新生成 figures。

## 当前重要结论

1. 几何信号在 RULER NIAH 上非常强。
2. VT 支持几何信号不只是“找特殊 needle”，它在状态跟踪任务中也有效。
3. attention 和 geometry 的表现随 budget 变化：tight budget 下 geometry 更像 evidence guard，宽预算下 attention 追上。
4. attention_l1 和 attention_l2 是当前最值得继续研究的 hybrid 路线。
5. compactor 在当前 MLX/RULER 设置下表现弱，可能与任务、预算、实现细节或保护 token 策略有关，需要单独分析而不是简单当成负例。

## 需要注意的语义

`snapkv`、`pyramidkv`、`compactor` 的当前 MLX 语义是：

```text
cache_budget_scope = prompt_prefill
```

也就是说预算主要约束 prefill 压缩后的 prompt cache，decode 阶段生成的新 token 会追加。因此这些方法的 `final_kv_len` 可能大于 `budget`，这不是 silent failure，结果字段里会记录 `cache_budget_scope` 和 `prefill_compression`。

普通 eviction 方法的预算语义是：

```text
cache_budget_scope = total_kv
```

这类方法应该满足 live KV 不超过 budget。

## 下一步建议

短期：

1. 先不要继续大规模 HotpotQA，之前的 partial 结果已删除。
2. 用固定 seed 随机抽样或分层抽样重跑 RULER NIAH/VT，避免 reviewer 质疑“只取前 N 条”。
3. 跑一个小规模 LongBench HotpotQA 或 MuSiQue，优先比较 `full / attention / l1_prefill_only / l2_prefill_only / attention_l1 / attention_l2`。
4. 做 selected-token overlap 和 rank-correlation 的论文图，重点展示 attention 与 leverage 选 token 的互补性。

中期：

1. 加入更严格的 prefill-only vs dynamic decode 消融。
2. 把 compactor-like hybrid 和标准 compactor 做清楚对齐。
3. 设计更数学化的 leverage score 解释，避免只靠 benchmark 分数叙事。
4. 做跨模型复现：Qwen2.5-7B、Llama-3.1-8B、Mistral-7B。

当前可以支撑的论文叙事：

> 几何结构是 KV cache token importance 的独立信号源，并且在 tight-budget evidence retention 中表现出 attention 难以替代的优势；attention 和 leverage 的 hybrid 是下一步提升真实任务表现的核心方向。
